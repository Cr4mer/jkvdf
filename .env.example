# FACEIT Data API key (server-side key).
# Create one at https://developers.faceit.com/ -> your app -> API Keys -> Server-side.
# Required by BOTH the ingest script (scripts/ingest.mjs) and the Go parser (parser/main.go).
FACEIT_API_KEY=
